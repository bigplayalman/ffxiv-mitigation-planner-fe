import { CalcDurationPipe } from './calc-duration.pipe';

describe('CalcDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
